%confusions
targets = gentarget(peak_data);
outputs = genoutputs(peak_data);
[c,cm,ind,per] = targets,outputs);
C = confusionmat()
[cb,cmb,indb,perb] = confusion(gentarget(peak_data),genbadoutput(peak_data));